# Дневник разработки (DEVLOG)

Здесь записано, что менялось, какие проблемы встречались и **как именно они
решались** (команды, скрипты), чтобы при повторении не разбираться с нуля.
Новые записи — сверху.

---

## Шпаргалка

### Как устроен проект
- `index.html` — кабинет учителя (один файл: разметка + стили + код).
- `cabinet.html` — кабинет родителя/ученика (открывается по личной ссылке,
  дальше запоминается на устройстве; «Забыть это устройство» — внизу).
- `firestore.rules` — правила доступа к базе Firestore. **Меняются в Git, но
  на сервер попадают только после команды деплоя** (см. ниже).
- `firebase.json` — настройки для команды `firebase` (где лежат правила,
  порты эмулятора).
- `tests/` — автотесты (в браузере с подменёнными Firebase/Cloudinary/CDN и
  проверка правил в локальном эмуляторе). На сайт не влияют.
- Вход учителя — **Firebase Authentication (email + пароль)**, данные в
  `teacherSpaces/{uid}`. Google-входа, чтения Google Календаря и Google
  Таблицы в проекте больше нет (с 2026-09-25).
- Сайт: GitHub Pages из ветки `main` → https://tviitterggl-dev.github.io/tutor-dashboard/
  (обновляется примерно через 1–2 минуты после push в `main`).

### Включить вход по email в Firebase (один раз, делает владелец проекта)
Консоль Firebase → проект `tutor-cabinet` → **Authentication** → «Get
started» (если ещё не нажимали) → вкладка **Sign-in method** → **Email/Password**
→ включить первый переключатель (Email/Password; «Email link» не нужен) →
**Save**. Проверка из облачной сессии (без пароля):
```
curl -s -X POST -H 'Content-Type: application/json' \
  "https://identitytoolkit.googleapis.com/v1/accounts:signInWithPassword?key=<apiKey>" \
  --data '{"email":"nobody@example.invalid","password":"x","returnSecureToken":true}' | jq .error.message
# CONFIGURATION_NOT_FOUND — Authentication не включён; OPERATION_NOT_ALLOWED — не включён Email/Password;
# EMAIL_NOT_FOUND / INVALID_LOGIN_CREDENTIALS — всё включено.
```
По желанию, после создания своей учётной записи: Authentication → Settings →
User actions → снять «Enable create (sign-up)», чтобы новые учётные записи
больше никто не заводил. Посторонние и так не видят чужих данных (правила
пускают только в свою папку), это просто порядок.

### Задеплоить правила Firestore (делает владелец Google-аккаунта)
В обычном терминале (PowerShell) на своём компьютере:
```
cd Desktop\tutor-dashboard
git pull
firebase deploy --only firestore:rules --project tutor-cabinet
```
Ждать строку `Deploy complete!`. Если `firebase` пишет, что не залогинен —
сначала `firebase login` (откроется браузер, войти под аккаунтом, где проект
`tutor-cabinet`). Если `firebase` не найден — `npm install -g firebase-tools`.

Из облачной сессии Claude Code это сделать нельзя: `firebase login` требует
входа в Google-аккаунт владельца, а сеть облачной сессии не пускает на
`auth.firebase.tools` (ответ прокси 403).

### Прогнать тесты (на любом компьютере с Node.js и Java)
```
cd tests
npm install
npm run test:rules   # правила в эмуляторе Firestore (нужна Java; эмулятор скачается сам)
npm run test:e2e     # сайт в Chromium с заглушками Google/Firebase/Cloudinary
node check-syntax.mjs ../index.html ../cabinet.html   # быстрая проверка синтаксиса
```
Для e2e нужен Chromium для Playwright (`npx playwright install chromium`, в
облачной сессии уже установлен).

### Проверить базу напрямую (без браузера)
Firestore REST API, ключ API — из `firebaseConfig` в `index.html` (он
публичный, это нормально для Firebase). Данные учителя без входа не
читаются: нужен токен (`idToken`) из входа учителя —
`accounts:signInWithPassword` с её email и паролем, из облачной сессии
этого не сделать. Без входа видны только витрины кабинетов и каналы, если
знать их ключи:
```
K=<apiKey>
B='https://firestore.googleapis.com/v1/projects/tutor-cabinet/databases/(default)/documents'
curl -s "$B/parentAccess/<ключ родителя>?key=$K" | jq '.fields | keys'
curl -s "$B/teacherSpaces?key=$K" | jq '.error.status'   # PERMISSION_DENIED
# с токеном учителя: curl -H "Authorization: Bearer $ID_TOKEN" "$B/teacherSpaces/$UID/state/main"
```
Важно: в bash путь с `(default)` надо брать в одинарные кавычки, иначе
`syntax error near unexpected token '('`.

### Модель данных Firestore
```
teacherSpaces/{uid}/state/main               отметки «Провёл» (marks), правки пакетов (pkgOverrides),
                                              профили учеников (studentProfiles: name, cls, rate,
                                              callUrl, accessUrl, notes), каналы (studentChannels)
teacherSpaces/{uid}/lessons/{id}              занятие: title, studentId ("Имя, N класс"), startMs/endMs,
                                              start/end (ISO, +03:00), date, time, durationMin,
                                              status: planned|done|cancelled|rescheduled,
                                              packageId, recurrenceId, source: gcal|app,
                                              report, homework[{url,name,by}], rescheduledFrom/To,
                                              paid {value,by,at}, callUrl (разовая ссылка на созвон)
teacherSpaces/{uid}/accessKeys/{key}          реестр выданных ключей: role, studentId, label,
                                              createdAt, active, revokedAt
teacherSpaces/{uid}/requests/{id}             решения по заявкам: type, lessonId, studentId, status
                                              approved|rejected, reason, old/newStartMs, decidedAt
parentAccess/{key}, studentAccess/{key}       «витрина» для кабинета: занятия одного ученика (с id),
                                              отчёты, домашка, пакет и paid (родителю), busy[] без
                                              имён, requests (решения), channel, parentChannel
channels/{ключ канала}/items/{id}             сообщения из кабинетов: homework | reschedule |
                                              cancel | paid (только создание, см. правила)
```
Занятия, когда-то импортированные из Google Календаря, имеют id = id
события, поэтому старые отметки «Провёл» (marks, тоже по id события)
продолжают работать.

Личное время хранится там же, в `lessons`, с полями `kind: "personal"`,
`note` (комментарий только для учителя) и `studentId: null`.

---

## 2026-09-25 (4) — установка приложения на Android: service worker и кнопка «Установить»

**Что сделано**

- `sw.js` — простой service worker. Всё берётся **сначала из сети** (обновления приходят
  сразу), а сохранённая копия отдаётся, только если сети нет: тогда оболочка всё равно
  открывается. Работает только со своим сайтом (Firebase, CDN и Cloudinary не трогает),
  только GET. Старые копии удаляются при смене `CACHE`.
- `index.html` регистрирует `sw.js`. Когда Chrome готов установить приложение
  (`beforeinstallprompt`), под заголовком появляется плашка «📲 Установить» / «Не сейчас»
  («Не сейчас» запоминается на устройстве). На iPhone такого события нет, поэтому
  плашка там не появляется: на iPhone ставят через «Поделиться» → «На экран «Домой»».
- `manifest.json`: добавлены `id` и `prefer_related_applications: false`. Значки 192×192
  и 512×512 были, есть и маскируемый (рисунок помещается в безопасный круг 80%).

**Как проверялось (живых телефонов нет — эмуляция в Chromium)**

- **Android** (профиль Pixel 7):
  - Chrome запущен с обычным профилем (`launchPersistentContext`) и с флагом
    `--bypass-app-banner-engagement-checks`. Без флага Chrome предлагает установку
    только после нескольких визитов.
  - Через протокол отладки Chrome (CDP) вызван `Page.getInstallabilityErrors`: ответ пустой,
    то есть Chrome считает приложение устанавливаемым.
  - `Page.getAppManifest` читает manifest без ошибок.
  - Пришло событие `beforeinstallprompt`, и появилась плашка «Установить».
  - Service worker управляет страницей; без сети страница перезагружается из копии.
- **iPhone** (профиль iPhone 13, движок Chromium, потому что WebKit в контейнере не установлен):
  - мета-теги на месте;
  - `apple-touch-icon` отдаётся как image/png 180×180;
  - manifest отдаётся;
  - плашки нет, страница не шире экрана.
  Само «На экран «Домой»» в Safari сэмулировать нельзя, поэтому проверено всё, что Safari при этом читает.

**Тестовый стенд**

- В `harness.mjs` service worker по умолчанию выключен (`serviceWorkers: "block"`), чтобы
  не влиять на остальные тесты.
- Добавлены опции `persistent` (обычный профиль в отдельном браузере) и `deviceScaleFactor`.
- Сервер теперь отдаёт `.png` и `.svg` с правильным типом.
- Был подводный камень: в Playwright контекст по умолчанию — «инкогнито», и Chrome
  отвечает `in-incognito`, то есть отказывается устанавливать приложение. Решение — `launchPersistentContext`.

Итог: e2e 53/53.
```
cd tests && npm run test:e2e
node --test --test-name-pattern="Android|iPhone|веб-приложение" e2e/personal.test.mjs
```

---

## 2026-09-25 (3) — «Личное время», смена класса ученика, установка на iPhone

**Что сделано**

1. **«Личное время»** (вкладка «Календарь»):
   - Как создать:
     - кнопка «+ Личное время» рядом с «+ Занятие»;
     - в окне нового занятия (после выделения времени в сетке) есть кнопка «Это не занятие — отметить «Личное время»».
   - Что можно указать: дата, время, длительность до 8 ч, комментарий «только для тебя», повтор каждую неделю.
   - Как выглядит: штрихованный серый блок «Личное время: комментарий».
   - Что с ним делать:
     - нажать, чтобы изменить или удалить (для серии: «это и следующие»);
     - перетащить на другое время — блок просто переезжает, без истории переноса.
   - Как хранится: документ в `lessons` с `kind: "personal"`. `fetchLessons()` по умолчанию
     его пропускает, поэтому он не попадает в деньги, прогноз, список занятий, пакеты, оплаты и .ics.
     Сетка календаря просит `includePersonal: true`.
   - Как видят кабинеты: витрины собирают `busy` из всех чужих занятий, и туда уходит
     только `{s, e}`. Родитель и ученик видят обычное «занято», без комментария и без
     слова «личное». Это проверяет тест.
2. **Смена класса** (вкладка «Ученики»): в карточке ученика рядом со ставкой появилось поле «Класс».
   - Перед сохранением приложение спрашивает подтверждение и пишет, сколько занятий переименуется.
   - `renameStudent()` переносит на новый id («Имя, N класс»):
     - названия и `studentId` всех занятий, прошлых и будущих;
     - профиль, пакет (`pkgOverrides`) и каналы;
     - `studentId` в ключах доступа и в журнале заявок;
     - после этого витрины перевыпускаются.
   - Что не меняется: ссылки родителя и ученика, каналы (ДЗ и заявки), отметки «Провёл» (они привязаны к id занятия).
   - Если ученик с таким именем и классом уже есть — отказ, ничего не меняется.
3. **Установка на iPhone**: `manifest.json` и значки `icons/*` (apple-touch-icon 180,
   192, 512, maskable, favicon). Добавлены мета-теги `apple-mobile-web-app-capable`,
   `apple-mobile-web-app-title` «Занятия», `theme-color` (светлая и тёмная тема).
   - Как установить: Safari → «Поделиться» → «На экран «Домой»».
   - У установленного приложения своё хранилище, поэтому в нём нужно один раз войти по email и паролю.
   - Значки нарисованы в `icons/icon.svg` и переведены в PNG через Playwright
     (скрипт `icons.mjs` в черновиках: открыть SVG в Chromium и сделать screenshot нужного размера).

**Правила Firestore** не менялись: `validLesson` пропускает дополнительные поля. В тесты
правил добавлена проверка записи личного времени.

**Тесты**:
- новый файл `tests/e2e/personal.test.mjs`, 5 тестов;
- всего e2e 51/51, правила 10/10.
```
cd tests && npm run test:e2e && npm run test:rules
node tests/check-syntax.mjs index.html cabinet.html
```

**Замечание**: кабинет родителя пока не настроен как веб-приложение, потому что ключ хранится в ссылке
(`#p=…`). Страница, открытая с экрана «Домой», ссылку не получит. Можно сделать позже.

---

## 2026-09-25 (2) — вход по email и паролю, без Google: .ics вместо экспорта

### Что сделано
1. **Вход учителя через Firebase Authentication (email + пароль)** вместо
   секретной ссылки `#t=КЛЮЧ`. Экран входа:
   - «Войти»;
   - «Забыли пароль?» — стандартное письмо Firebase, `sendPasswordResetEmail`;
   - «Первый вход — создать учётную запись» (`createUserWithEmailAndPassword`,
     прямо в браузере, без сервера), пароль не короче 8 символов.

   Вход запоминается на устройстве (обычное поведение Firebase). Кнопка
   «выйти» — в верхней карточке; в «Ученики» → «Аккаунт» — «Сменить пароль»
   и «Выйти».
2. **Данные переехали в `teacherSpaces/{uid}`**, правила переписаны на
   `request.auth.uid == uid` (функция `isTeacher`). Секретный ключ в пути
   больше не нужен: старые пути `teacherSpaces/{ключ}` не совпадают ни с
   одним uid и закрываются сами, как только задеплоены новые правила.
3. **Перенос данных при первом входе.** Если в новой учётной записи данных
   нет, дашборд копирует всё со старого пути: `state/main`, `lessons`,
   `accessKeys`, `requests`; `state/main` — последним, чтобы незавершённый
   перенос не выглядел готовым. Старый ключ берётся из памяти устройства,
   где раньше открывали секретную ссылку; если его там нет, дашборд просит
   вставить старую ссылку. Работает, **пока на сервере старые правила** —
   поэтому порядок: сначала первый вход на сайте, потом деплой правил. Старые
   данные не удаляются (резервная копия; после деплоя правил они недоступны
   никому). Есть «Начать с пустого дашборда» — на случай, если переносить
   нечего.
4. **«Добавить в календарь (.ics)»** вместо экспорта в Google Календарь.
   Скачивается файл события (RFC 5545):
   - время в UTC;
   - в описании — ссылка на созвон и отчёт;
   - напоминание за 30 минут.

   Телефон или компьютер сам предлагает добавить событие в Google, Apple или
   Outlook, без входа. UID каждый раз новый, поэтому повторное нажатие
   добавляет ещё одно событие (так договорились).
5. **Google OAuth удалён полностью**: Google Identity Services, OAuth Client
   ID, чтение календаря «Занятия» (с откатом на него и кнопками
   «Досинхронизировать» / «Вернуться к Google Календарю»), чтение
   основного календаря в «Расписании» и в «занято» кабинетов, экспорт
   через Google-вход, **чтение Google-таблицы ставок**.
6. **Ставки теперь в профилях учеников** (вкладка «Ученики»: поле «Ставка за
   занятие»). Перед переходом я прочитала таблицу «Ставки учеников» через
   Google Drive и записала 17 ставок в `studentProfiles`, не трогая ссылки и
   заметки: скрипт `rates_migrate.mjs` в облачной сессии, `PATCH
   …/state/main?updateMask.fieldPaths=studentProfiles`. Список учеников
   (`studentList()`) — это профили; удаление ученика удаляет профиль целиком.

### Что стало хуже / о чём помнить
- «Расписание» и «занято» в кабинетах больше не учитывают **личные события**
  из Google Календаря: без Google-входа их не прочитать. Сейчас там только
  занятия.
- Кто угодно может завести учётную запись на сайте, но увидит только свою
  пустую папку. Чтобы закрыть регистрацию — см. шпаргалку («Включить вход
  по email»).

### Порядок шагов владельца (важно!)
1. Консоль Firebase → Authentication → Get started → Sign-in method →
   Email/Password → включить → Save. Пока не включено, экран входа пишет
   «Вход по email ещё не включён в Firebase».
2. Открыть сайт → «Первый вход» → email и пароль → вставить **новую
   ссылку для переноса** (выдана в чате 2026-09-25 после инцидента ниже;
   старая ссылка с сохранённым ключом устарела) → «Готово: перенесено
   занятий — N…».
3. Только после этого: `git pull` → `firebase deploy --only firestore:rules
   --project tutor-cabinet`.
4. На остальных устройствах — просто войти с тем же email и паролем.

**Если правила задеплоили раньше шага 2** (экран скажет «Старые данные уже
закрыты новыми правилами…»): временно вернуть старые правила, сделать
шаг 2, затем снова задеплоить новые:
```
git show 49a35d8:firestore.rules > firestore.rules
firebase deploy --only firestore:rules --project tutor-cabinet
# … первый вход на сайте, дождаться «Готово: перенесено…»
git checkout firestore.rules
firebase deploy --only firestore:rules --project tutor-cabinet
```

### Инцидент: действующий ключ попал в публичный репозиторий (исправлено)
- **Что случилось.** В тест правил (`tests/rules/rules.test.mjs`, константа
  `OLD_T`) я вписала **настоящий действующий ключ учителя** — как пример
  «старого пути». Коммит `3d067aa` ушёл в публичный `main`. Пока на сервере
  старые правила, этот ключ давал полный доступ к данным.
- **Как нашли.** Перед каждым коммитом проверяю staged-файлы командой
  `git diff --cached --name-only | xargs grep -lE "<начало ключа>|github_pat"`.
  В этот раз она сработала **после** коммита — проверку я запустила, но
  результат просмотрела уже после push.
- **Что сделано** (через несколько минут):
  1. ключ убран из теста (вымышленное значение);
  2. данные скопированы на **новый секретный путь**, которого нет в
     репозитории: REST-скрипт, 210 занятий, 4 ключа доступа, состояние;
     сверено поле-в-поле, резервная копия в облачной сессии;
  3. по засвеченному пути все 215 документов **удалены** — ключ из истории
     Git больше ничего не открывает. После деплоя новых правил путь закрыт
     совсем;
  4. в дашборде: если сохранённый на устройстве старый ключ устарел, он
     забывается, и экран просит вставить актуальную ссылку;
  5. новый тест «в репозитории нет настоящих ключей»: ищет sha256 всех
     32-символьных слов в основных файлах (самого ключа в тесте нет).
- **Правило на будущее:** настоящие ключи, uid, пароли и email владельца не
  пишем ни в код, ни в тесты, ни в DEVLOG. Для примеров — только
  вымышленные значения. Проверку staged-файлов на секреты — **до** коммита,
  и не пушить, пока она не пустая.

### Проблемы и решения
- **Firebase Authentication в проекте не был включён** — проверила запросом
  `accounts:signInWithPassword` на несуществующий email: ответ
  `CONFIGURATION_NOT_FOUND` (команда в шпаргалке). Включить можно только в
  консоли под владельцем — это шаг 1.
- **Правила с `request.auth` в эмуляторе.** Эмулятор принимает неподписанный
  JWT (`alg: none`) с `sub`/`user_id` = uid. Функция `tokenFor(uid)` в
  `tests/rules/rules.test.mjs`. Проверено: учитель пускается; без входа и
  под чужим аккаунтом — 403; чужой может завести только свою папку; старые
  пути с ключом закрыты для всех, включая вошедшего учителя.
- **Тестовый стенд:**
  - новая заглушка `tests/stubs/firebase-auth.js` (пользователи и вход
    хранятся в localStorage);
  - заглушка Firestore повторяет новые правила: `teacherSpaces/{uid}` —
    только вошедшему с этим uid; `window.__FAKE_RULES = "old"` включает
    старые правила для теста переноса;
  - занятия больше не «импортируются из календаря», а сразу лежат в базе:
    `lessonDoc()` в `harness.mjs`;
  - `.ics` проверяется через `page.waitForEvent("download")`: время,
    экранирование, перенос строк ≤ 75 байт, новый UID при повторе.
- **Порядок запуска.** Модуль Firebase выполняется после основного скрипта,
  а первый `onAuthStateChanged` может прийти раньше подписки. Решение:
  флаг `TutorAuth.ready` + `authReady()`, плюс защита от двойного запуска
  (`appStarting`).

### Тесты
46 e2e-сценариев (новый `app.test.mjs`: вход, неверный пароль, «забыли
пароль», выход, первый вход с автопереносом, перенос по вставленной ссылке,
устаревший ключ на устройстве, «правила уже новые», «начать с пустого»,
занятый email, «вход не включён», в коде нет Google, в репозитории нет
настоящих ключей) и 10 проверок правил — всё зелёное.

---

## 2026-09-25 — ученики: добавить/удалить, созвон в кабинете, «Оплачено», пакеты, сводка оплат

### 1. Добавление и удаление учеников (вкладка «Ученики»)
- Раньше список строился только из таблицы «Ставки учеников» (Google
  Sheets). Теперь есть «+ Добавить ученика»: имя, класс, ставка
  (необязательно).
  - Имя — **одним русским словом**: занятия распознаются по названию
    «Имя N класс» (`parseLesson`), иначе ни ставка, ни пакеты не найдутся.
  - Хранится в `state/main.studentProfiles["Имя, N класс"]` с `manual: true,
    name, cls, rate`.
  - Ставка ручного ученика используется, если его нет в таблице (запасной
    вариант в `findRate`).
  - Общий список учеников — функция `studentList()` (таблица + ручные − скрытые).
    Он же используется в выборе ученика при создании занятия и при выдаче доступа.
- «Удалить ученика…» открывает окно с подсчётом того, что пропадёт:
  - удаляются профиль (ссылки, заметки), будущие занятия и ручные правки пакета;
  - все активные доступы отзываются: витрины удаляются, ключи помечаются
    `revokedReason: "student-deleted"`;
  - каналы очищаются и отвязываются.

  Прошедшие занятия по умолчанию **остаются** — в истории и в заработке.
  Галочка «Удалить и прошедшие» удаляет и их, вместе с отметками «Провёл»
  (одним `updateDoc` на пачку полей `marks.<id>`).
- Если ученик есть в таблице ставок, у нас нет прав её менять (доступ только
  на чтение). Поэтому ставим ему в профиле отметку «скрыт» (`hidden: true`),
  а в окне пишем «строку в таблице удали вручную». Добавить его заново с тем
  же именем и классом — отметка снимается.
- Сохранение профиля теперь **дописывает** поля к уже сохранённым, а не
  заменяет профиль целиком. Иначе заметка стирала бы у ручного ученика имя,
  класс и ставку.

### 2. Баг: ссылка на созвон не видна родителю/ученику
**Причина:** в прошлый раз по договорённости «поля профиля видит только
учитель» в витрину кабинета не шло ни одно поле профиля, в том числе ссылка
на созвон. Код работал «как задумано», но задумка не подходила для созвона.
**Исправление:**
- в каждое занятие витрины (кроме отменённых и перенесённых) добавлено
  `callUrl`. Выбор ссылки — `callLinkFor(l)`: сначала разовая у занятия,
  потом из профиля;
- после сохранения профиля или разовой ссылки кабинеты сразу обновляются
  (`publishViewsSoon()`; раньше этого вызова не было — ещё одна причина);
- в кабинете: кнопка «Подключиться к занятию» в окне занятия и ссылка
  «Подключиться к занятию →» у ближайших уроков в ленте.

**Заметки и «ссылка на материалы» по-прежнему видны только учителю** — это
проверяет тест.

### 3. «Оплачено» — кнопка в стиле «Провёл»
Вместо галочки — кнопка `mark-btn paid-btn`, та же, что «Провёл» (у учителя
и у родителя). Когда оплачено: «✓ Оплачено (снять)», светло-зелёный фон и
зелёная обводка 2px.

### 4. «Пакет заканчивается»
- Правило: осталось 1–2 занятия — жёлтая подсветка «пора предложить
  продление»; 0 — красная «пакет закончился» (функция `pkgEnding()`).
- Где видно:
  - плашка наверху кабинета учителя на любой вкладке, по нажатию — на
    «Учеников»;
  - подсвеченная карточка пакета;
  - метка у ученика в списке;
  - у родителя — предупреждение под прогрессом пакета.
- Плашка пересчитывается после отметок «Провёл» и любых изменений занятий
  (`refreshPackageAlertsSoon()`, с задержкой 0,8 с).

### 5. «Итоги» → «Оплата»
Карточка под итогами за выбранный период (неделя/месяц/свой):
- **Не оплатили** — проведённые занятия («Провёл») без отметки «Оплачено»;
- **Оплатили** — занятия с отметкой.

По каждому ученику: количество занятий, сумма, даты; вверху каждого списка —
общая сумма. Работает, когда занятия ведутся в дашборде.

### Тесты
39 e2e-сценариев и 10 проверок правил — всё зелёное. Добавлено:
- `students.test.mjs` — ручной ученик в списках и со ставкой; удаление
  ученика из таблицы (будущее убрано, прошлое осталось, доступ закрыт,
  можно вернуть); удаление вместе с прошлым; «пакет заканчивается» у
  учителя и у родителя; сводка оплат;
- `profiles.test.mjs` — ссылка на созвон появляется в открытом кабинете
  после сохранения профиля, разовая ссылка заменяет её только у одного
  занятия, заметки и материалы в витрину не уходят.

Проблема в тестах: `toLocaleString("ru-RU")` разделяет тысячи неразрывным
пробелом, поэтому в регулярках нужно `1\s700`, а не `1 700`.

Правила базы **не менялись** (всё новое живёт в `state/main` и в занятиях).

---

## 2026-09-24 (поздний вечер) — профили учеников, вкладка «Заявки», чистка тестовой заявки

### 1. Профили учеников (вкладка «Ученики» у учителя)
- Карточка «Ученики» теперь первая на вкладке. Ученик раскрывается нажатием,
  внутри три поля:
  - **ссылка на созвон** (постоянная);
  - **ссылка на занятие/доступ** (доска, папка — необязательно);
  - **заметки** (что прошли, что дальше).

  Кнопки: «Сохранить», «Открыть созвон», «Материалы».
- Где хранится: `state/main.studentProfiles["Имя, N класс"] = {callUrl,
  accessUrl, notes, updatedAt}`. Запись точечная
  (`updateDoc(ref, new FieldPath("studentProfiles", id), …)`). Документ
  `state/main` доступен только по ключу учителя, поэтому **правила базы не
  менялись**. В витрины родителей и учеников эти поля не попадают — это
  проверяет тест.
- **Ссылка на созвон в занятии.** Отдельного поля «ссылка» у каждого занятия
  не вводим — она берётся из профиля ученика автоматически. Для одного
  занятия её можно заменить: в карточке занятия блок «Созвон» →
  «Сохранить для этого занятия» пишет `lesson.callUrl`; «Вернуть обычную»
  возвращает ссылку из профиля. Порядок выбора — функция `callLinkFor(l)`:
  сначала `lesson.callUrl`, потом профиль. В списке «Занятия» рядом с уроком
  появилась быстрая ссылка «созвон».
- Ссылки принимаются только вида `https://` / `http://` (защита от
  `javascript:`).

### 2. Кабинет: 5-я вкладка «Заявки»
- Карточка «Последняя заявка» убрана из «Занятий» совсем.
- Новая вкладка «Заявки»:
  - блок «Ждут ответа» — сразу из канала;
  - «История заявок» — всё решённое за год: что просили (было → стало или
    отмена), комментарий, кто подал и когда, ответ преподавателя (дата,
    подтверждено/отклонено, причина).

  Красная точка «есть заявка без ответа» переехала на эту вкладку.
- Учитель теперь кладёт в витрину решения за 365 дней (было 60) и больше
  полей: `createdAt`, `comment`, `oldEndMs`, `newEndMs`. Не больше 200 решений
  на ученика — чтобы витрина не росла бесконечно.
- Кабинет учителя на телефоне: вкладок стало шесть, подписи наезжали друг на
  друга. Теперь на экране уже 620px вкладки не сжимаются (`flex: 1 0 auto`),
  а полоса вкладок прокручивается вбок.

### 3. Удалена тестовая заявка из боевой базы
В журнале была ровно одна заявка — тестовая: перенос «Сева, 7 класс» с
Чт 24.09 16:00 на Вс 27.09 19:00, «подтверждено», id `f690713d5c9e7be6f8`.
Расписание она к этому моменту уже не затрагивала: исходного занятия и
занятия на 27.09 в базе нет. Поэтому удалили только саму запись и её копии
в витринах Севы. Команды (REST, из облачной сессии):
```
# резервная копия
curl -s "$B/teacherSpaces/$T/requests/$R?key=$K" > backup_request.json
# удалить запись журнала
curl -s -X DELETE "$B/teacherSpaces/$T/requests/$R?key=$K"
# очистить список заявок в витринах ученика (родитель и ученик)
curl -s -X PATCH -H 'Content-Type: application/json' \
  --data '{"fields":{"requests":{"arrayValue":{"values":[]}}}}' \
  "$B/parentAccess/$KEY?key=$K&updateMask.fieldPaths=requests"   # и studentAccess/$KEY
```
Как найти занятия ученика запросом (для проверки):
```
curl -s -X POST -H 'Content-Type: application/json' "$B/teacherSpaces/$T:runQuery?key=$K" \
  --data '{"structuredQuery":{"from":[{"collectionId":"lessons"}],"where":{"fieldFilter":{"field":{"fieldPath":"studentId"},"op":"EQUAL","value":{"stringValue":"Сева, 7 класс"}}}}}'
```
Сейчас журнал заявок пуст.

### Тесты
33 e2e-сценария и 10 проверок правил — всё зелёное. Добавлено:
- `profiles.test.mjs`:
  - сохранение профиля, неверная ссылка не принимается, данные на месте
    после перезагрузки;
  - быстрая ссылка «созвон» в «Занятиях»;
  - подстановка ссылки в карточку, разовая замена, «вернуть обычную»,
    соседнее занятие не задето;
  - ничего из профиля не попадает в витрину родителя;
- «вкладка „Заявки“ в кабинете» — подтверждённая и отклонённая заявки
  с датами, комментарием и причиной.

Одна проверка на загрузку ДЗ иногда «моргала»: тест читал базу в момент,
когда учитель переносит файл из канала в занятие. Проверка переписана на
ожидание — файл допустимо найти и в канале, и в занятии.

---

## 2026-09-24 (вечер) — кабинет родителя/ученика разбит на вкладки

### Что сделано
Длинная страница `cabinet.html` разбита на 4 вкладки. Переключатель вкладок
стоит под шапкой с именем ученика и остаётся наверху при прокрутке
(`position: sticky`).
1. **Занятия** (открыта по умолчанию). Порядок блоков:
   - у родителя — «Пакет занятий»;
   - статус заявки: все, что ждут ответа, а если таких нет — последняя решённая;
   - одна лента с переключателем «Ближайшие (N) / История (N)».

   Пока есть заявка без ответа, на вкладке горит красная точка.
2. **Календарь** — прежняя сетка день/неделя/список с переносом и отменой
   по тапу и перетаскиванием.
3. **ДЗ** — загрузка без поиска по датам. Занятие выбирается в списке
   (занятия за 3 недели назад и 2 вперёд; по умолчанию — последнее
   прошедшее). Ниже — лента занятий, где есть файлы, у каждой карточки кнопка
   «+ добавить файл».
4. **Ещё** — контакты «Если что — пишите», «Забыть это устройство» и время
   последнего обновления расписания.

Логику данных не меняли: те же витрины, каналы, проверка чужих занятий у
учителя и живые подписки `onSnapshot`. Изменилась только раскладка.

### Технические детали
- Календарь теперь создаётся **один раз** — при первом открытии вкладки.
  Скрытый контейнер FullCalendar меряет с нулевой шириной, поэтому
  создавать календарь заранее нельзя. Живые обновления больше не
  пересоздают его, а только меняют события: `removeAllEventSources()` +
  `addEventSource()`. Отсюда меньше мерцания, и выбранная неделя не
  сбрасывается. При смене ребёнка календарь уничтожается (`cal.destroy()`).
- Загрузка ДЗ вынесена в общую `uploadFiles()`: её используют окно занятия и
  вкладка «ДЗ». Пока файл грузится, ленту ДЗ не перерисовываем (флаг
  `uploading`) — иначе живое обновление заменило бы элемент выбора файла
  посреди загрузки.

### Тесты
Старые тесты переведены на вкладки: календарь теперь открывается кнопкой
вкладки, прошедшие занятия лежат под фильтром «История». Добавлены два теста:
- переключение вкладок, фильтр ленты, загрузка ДЗ через выбор занятия и
  через «+ добавить файл», вкладка «Ещё»;
- телефон 360px: ни на одной вкладке нет горизонтальной прокрутки.

Итого 30 e2e-сценариев и 10 проверок правил — всё зелёное.
Правила базы в этот раз **не менялись**, деплоить ничего не нужно.

---

## 2026-09-24 (день) — доработки кабинета родителя/ученика по итогам проверки

### Состояние на начало
Правила из ночной сессии задеплоены владельцем, автоимпорт прошёл (210
занятий в `lessons`), старый ключ закрыт (`PERMISSION_DENIED`), выданы два
тестовых доступа (родитель + ученик).

### Что сделано
1. **Прошлая неделя в кабинете.** Витрина теперь начинается с понедельника
   прошлой недели (`busyFrom`) и идёт на 5 недель вперёд; календарь кабинета
   ограничен этим диапазоном (`validRange`), кнопка «назад» дальше не пускает.
2. **«Забыть это устройство»** внизу `cabinet.html`. Ссылка `#p=…`/`#s=…` при
   первом открытии сохраняется в `localStorage["cabinetKeys"]` и убирается из
   адресной строки; дальше кабинет открывается и без ссылки. Кнопка стирает
   всё сохранённое. Если у родителя несколько детей (несколько ссылок) —
   сверху появляется переключатель.
3. **Интерактивный календарь в кабинете (FullCalendar) + заявки.**
   Родитель и ученик НЕ меняют расписание: «Перенести…» (выбор даты/времени в
   окне по тапу — удобно на телефоне) и перетаскивание урока мышкой/долгим
   нажатием создают **заявку**; «Отменить…» — заявку на отмену (с
   комментарием). Занятое время для переноса не принимается. У учителя —
   вкладка **«Заявки»** со счётчиком на вкладке, красной плашкой «Новые
   заявки: N — открыть» в верхней карточке и `(N)` в заголовке вкладки
   браузера. «Подтвердить» переносит (с историей «перенесено») или отменяет
   занятие, «Отклонить» спрашивает причину. Решение сразу видно в кабинете.
4. **Контакты «Если что — пишите»**: Telegram `https://t.me/mat_repet` и
   Яндекс Телемост (ссылка-приглашение) — карточка внизу кабинета.
5. **ДЗ из кабинетов.** Родитель и ученик загружают файлы (Cloudinary, тот же
   unsigned preset). Файл сразу виден всем троим: в кабинетах — из канала
   мгновенно, у учителя — открытый дашборд переносит его в занятие
   (`lesson.homework[]`, с пометкой, кто загрузил). Порядок важен: сначала
   обновляются витрины, потом сообщение удаляется из канала — иначе файл
   «мигнул» бы и пропал.
6. **«Оплачено»** — галочка у родителя (в окне занятия) и у учителя (в
   карточке занятия + метка «оплачено» в списке «Занятия»). Хранится в
   облаке, видна обеим сторонам сразу. Ученику галочка и отметки не
   показываются и в его витрину не передаются.
7. **Приватность проверена тестами** (см. ниже).

### Как устроено (каналы)
Писать в базу родителю/ученику можно только в «каналы»:
`channels/{ключ канала}/items/{id}`. У каждого ученика два канала со
случайными ключами (хранятся у учителя в `state/main.studentChannels`):
- `shared` — общий для родителя и ученика этого ребёнка: ДЗ и заявки;
- `parent` — только для родителей: «оплачено».
Ключи каналов попадают только в витрины этого ученика (`view.channel`, а
`view.parentChannel` — только в витрину родителя). Правила: сообщения можно
только создавать (изменять нельзя), состав полей ограничен, ссылки на файлы —
только `https://res.cloudinary.com/...`. Кабинеты подписаны на свою витрину и
каналы через `onSnapshot` (обновляются без перезагрузки), дашборд учителя —
на каналы всех учеников с выданным доступом.

Учитель проверяет **каждое** сообщение: занятие должно существовать и
принадлежать ученику этого канала. Иначе: ДЗ/«оплачено» выбрасываются,
заявка автоматически отклоняется с причиной «Занятие не найдено», и в
решение не пишется ничего о чужом занятии (`oldStartMs: null`).
«Оплачено», пришедшее через общий канал (то есть мог поставить ученик), тоже
выбрасывается. Журнал решений — `teacherSpaces/{ключ}/requests/{id}`,
в витрину попадают только решения по этому ученику за 60 дней.

**Отзыв доступа** теперь ещё и меняет ключи каналов ученика (новые каналы,
нерешённые заявки и отметки переносятся, старые каналы очищаются) —
отозванный родитель/ученик больше не читает и не пишет в канал.

### Проблемы и как решались
- **Кабинет: `FullCalendar is not defined`.** Кабинет перерисовывается при
  каждом живом обновлении; второй вызов загрузки скрипта находил тег
  `<script>` и считал библиотеку загруженной, хотя она ещё грузилась. Решение
  (и в `cabinet.html`, и в `index.html`): один общий промис на каждый скрипт
  (`scriptLoads[src]`), плюс повторная проверка `if (fc) return fc` после
  `await` в `ensureCalendar()`.
- **Кабинет: после смены канала старая подписка затирала данные** пустым
  списком (старый канал очищается при отзыве чужого доступа). Решение:
  при смене `view.channel` отписываться от старого канала и игнорировать
  ответы «не своего» канала.
- **Правила ещё не задеплоены** → запись в канал получает
  `permission-denied`. В кабинете вместо «проверьте интернет» теперь:
  «Эта функция ещё не включена у преподавателя. Пока напишите в Telegram».
- **Стенд: параллельные записи из двух вкладок** в общий localStorage
  («фейковая база») иногда теряли одну из записей. На настоящем Firestore
  такого нет; в тесте приватности «подделки» пишутся через вкладку учителя.
- **Стенд: `textContent` у `body` включает исходный код `<script>`** — для
  проверок «на экране нет слова …» нужен `innerText`.
- **Стенд: FullCalendar рисует только видимую неделю/день** — заявка на
  следующую неделю видна после «›»; на телефоне вид «день» (сегодня).

### Тесты
`npm run test:rules` — 10 проверок (добавлены каналы и журнал заявок),
`npm run test:e2e` — 28 сценариев (добавлен `family.test.mjs`: прошлая
неделя, «Забыть устройство», заявки по тапу и перетаскиванием, отказ с
причиной, ДЗ ученика → родитель → учитель, «Оплачено» в обе стороны,
приватность, смена каналов при отзыве, сообщение до обновления правил).

### Что нужно от владельца (нужен её логин)
1. **Обновить правила базы** — без этого заявки, загрузка ДЗ из кабинетов и
   «Оплачено» у родителя не заработают (кабинет честно скажет, что функция
   ещё не включена):
   ```
   cd Desktop\tutor-dashboard
   git pull
   firebase deploy --only firestore:rules --project tutor-cabinet
   ```
2. Открыть свой дашборд один раз после деплоя — он заведёт каналы для
   учеников с выданными доступами и обновит их кабинеты.

---

## 2026-09-24 — ночная сессия: редактор занятий, кабинеты, безопасность

### Что сделано
1. **Безопасность: ключ учителя убран из кода сайта.** Репозиторий и сайт
   публичные, а старый ключ (`TEACHER_ID`) был прямо в `index.html` — любой
   мог открыть исходник страницы и получить полный доступ к данным. Теперь:
   - ключ попадает на устройство один раз — через секретную ссылку
     `https://tviitterggl-dev.github.io/tutor-dashboard/#t=КЛЮЧ` (ключ
     сохраняется в localStorage, а из адресной строки сразу стирается) или
     ввод на экране «Ключ учителя»;
   - выпущен **новый ключ**, данные (`state/main`) скопированы на него
     (см. «Как переносили данные» ниже);
   - в `firestore.rules` старый ключ закрыт полностью (после деплоя правил).
   - Ключ не хранится в репозитории. Если потеряется — его видно в консоли
     Firebase (Firestore → Data → `teacherSpaces`), вход под своим Google.
2. **Коллекция `lessons` и разовый импорт из календаря «Занятия».**
   Импорт запускается **сам** при первом открытии дашборда после деплоя
   новых правил (или кнопкой во вкладке «Календарь»). Берутся события до
   31.10.2026. Повторный запуск («Досинхронизировать») добавляет только новые
   события и не трогает уже перенесённые. Есть кнопка «Вернуться к Google
   Календарю» (данные в дашборде при этом не удаляются).
   Пока правила не задеплоены, дашборд работает как раньше — от Google
   Календаря (при ответе базы `permission-denied` автоматически откат).
3. **Вкладка «Календарь» (FullCalendar 6.1.19, CDN jsdelivr):** день/неделя/
   месяц/список; создание занятия (в т.ч. еженедельная серия и пакет «1/8…»),
   проверка пересечений, перетаскивание = перенос (с подтверждением),
   растягивание = длительность. Карточка занятия: оплата/«Провёл»,
   «Сохранить» (исправить на месте, для серии — «это и все следующие»),
   «Перенести» (старое → `rescheduled`, новое создаётся, история видна
   родителям), «Отменить» (одно/серия), «Вернуть», «Удалить».
4. **Отчёт по занятию** (текст) и **файлы домашки** через Cloudinary
   (cloud `xf4hvf5p`, unsigned preset `tutor-dashboard`, до 10 МБ на файл).
   «Убрать» файл — только отвязывает ссылку (unsigned-загрузку нельзя удалить
   из браузера; удалять из Cloudinary — в его консоли, если нужно).
5. **Экспорт в личный Google Календарь** (одна сторона): кнопка в карточке
   занятия. Права на запись (`calendar.events`) запрашиваются только по этой
   кнопке — Google покажет окно согласия один раз. Повторный экспорт
   обновляет то же событие (PATCH), а не создаёт дубликат.
6. **Кабинеты родителя и ученика** (`cabinet.html#p=КЛЮЧ` / `#s=КЛЮЧ`):
   ближайшие занятия, история с отчётами и домашкой, пакет (только родителю),
   сетка занятости на 5 недель: свои уроки выделены, чужие слоты и личные
   события — «занято» без имён. **Управление ключами** — вкладка «Ученики» →
   «Доступы»: выдать (ссылка + «Скопировать»/«Поделиться»), отозвать, история.
   Витрины обновляются сами через ~2,5 с после любых изменений и при каждом
   открытии дашборда.
7. **PNG расписания на iPhone:** вместо `window.open` (Safari блокирует его
   после `await`) картинка показывается в окне на странице: зажать → «Сохранить
   в Фото», или кнопка «Поделиться» (`navigator.share` с файлом).
8. **Отметки больше не затираются между устройствами.** Раньше каждое
   сохранение перезаписывало весь документ `state/main` (`setDoc`) — вкладка,
   открытая на втором устройстве, стирала отметки первого. Теперь пишется одно
   поле: `updateDoc(ref, new FieldPath("marks", id), value)`.
9. Постраничная загрузка событий Google Календаря (`pageToken`) — раньше
   молча обрезалось на 250 событиях.
10. Тесты: `tests/rules` (7 проверок правил в эмуляторе) и `tests/e2e`
    (18 сценариев в Chromium). Все зелёные.

### Проблемы и как решались
- **Секрет в публичном коде.** Решение описано выше. Проверка, что ключа нет
  в коде: тест `access.test.mjs` → «в коде сайта нет ключа учителя».
- **Как переносили данные на новый ключ** (REST, из облачной сессии —
  Firestore туда пускает; старые правила разрешали запись в `state/*` по
  любому ключу):
  ```
  curl -s "$B/teacherSpaces/$OLD/state/main?key=$K" > backup.json          # резервная копия
  curl -s -X PATCH -H 'Content-Type: application/json' \
       --data "{\"fields\": $(jq .fields backup.json)}" \
       "$B/teacherSpaces/$NEW/state/main?key=$K"                           # копия на новый ключ
  ```
  После деплоя новых правил старый путь закрывается (`liveTeacher()` в правилах).
- **Код новый, а правила на сервере старые** → `listLessons` падает с
  `permission-denied`. Решение: `fetchLessons()` ловит это и показывает
  занятия из Google Календаря, а над календарём — подсказку про деплой.
  Автоимпорт проверяет доступ (`lessonsAllowed()`) и сам стартует, когда
  правила обновлены.
- **Составные индексы Firestore.** Запрос «recurrenceId == X и startMs ≥ Y»
  потребовал бы создавать индекс в консоли. Обход: серия берётся только по
  `recurrenceId ==`, а фильтр по дате делается в браузере; диапазоны — только
  по одному полю `startMs` (индекс создаётся автоматически).
- **`firebase login` из облачной сессии невозможен** (прокси 403 на
  `auth.firebase.tools`, и это вход в чужой Google-аккаунт) — деплой правил
  делает владелец со своего компьютера (шпаргалка сверху).
- **Эмулятор Firestore** в облаке скачивается нормально:
  `firebase setup:emulators:firestore` (jar с `storage.googleapis.com`, нужна
  Java 11+). Предупреждения `EAFNOSUPPORT ::1` — безвредны (нет IPv6).
- **`node --test tests/rules/` в Node 22 падает** («Could not find») —
  нужен шаблон файлов: `node --test "tests/rules/*.test.mjs"`.
- **Тестовый стенд, протухший токен:** `page.clock.setFixedTime()` действует
  после init-скриптов контекста, поэтому `Date.now()` при подготовке
  localStorage был реальным, а в приложении — фиксированным (другим).
  Решение: срок токена в стенде — на 10 лет вперёд.
- **Стенд, вторая вкладка стирала «базу»:** флаг «уже засеяно» лежал в
  sessionStorage (он свой у каждой вкладки). Перенесён в localStorage.
- **Перетаскивание в Playwright не срабатывало:** событие было ниже видимой
  части окна (календарь `height: auto`). Решение: `scrollIntoView` перед
  `mouse.down()` и движение мелкими шагами.
- **Сообщение «Готово: добавлено …» затиралось** перерисовкой календаря —
  поменян порядок вызовов в `runImport()`.
- **Проверка на настоящих данных без риска:** в песочнице (не в репозитории —
  там настоящие имена) импорт прогнан на 210 реальных событиях и 93 реальных
  отметках: 210/210 перенесено, 77 отметок → `done`, 7 пакетов, 6 пробных
  занятий без ученика, ошибок нет.

### Что осталось сделать владельцу (нужен её логин)
1. Открыть на каждом своём устройстве **новую секретную ссылку** (выдана в
   чате, в репозиторий не попадает) — один раз.
2. Задеплоить правила (шпаргалка сверху): `git pull` →
   `firebase deploy --only firestore:rules --project tutor-cabinet`.
   После этого при следующем открытии дашборда занятия перенесутся сами.
3. При первом «Добавить в мой Google Календарь» Google спросит разрешение на
   запись в календарь — нажать «Разрешить».

---

## 2026-09-23 — перенос отметок в Firestore, первые правила

- `index.html` заменён на версию, где отметки «Провёл» и ручные правки
  пакетов хранятся в Firestore (`teacherSpaces/{ключ}/state/main`) вместо
  localStorage — видны со всех устройств.
- Добавлены `firestore.rules` и минимальный `firebase.json`.
- Проверка синтаксиса HTML-файла: вытащить `<script>` и прогнать
  `node --check` (для `type="module"` — сохранить как `.mjs`;
  `node --input-type=module --check файл` **не работает** — флаг только для
  stdin). Теперь это делает `tests/check-syntax.mjs`.
- Правила задеплоены владельцем со своего компьютера (PowerShell):
  `npm install -g firebase-tools` → `firebase login` (на вопросы про Gemini и
  сбор статистики можно ответить `n`) → `firebase deploy --only
  firestore:rules --project tutor-cabinet` → `Deploy complete!`.
- Слияние в `main` сделано fast-forward'ом из рабочей ветки
  (`git checkout -B main origin/main && git merge --ff-only <ветка> && git push origin main`).
